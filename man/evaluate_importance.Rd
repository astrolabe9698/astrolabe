% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{evaluate_importance}
\alias{evaluate_importance}
\title{Evaluate Variable Importance and Decide Removability}
\usage{
evaluate_importance(
  df,
  imp_vec,
  predictors,
  importance_method = c("fixed", "net_clust", "neg_exp"),
  quantitative_thr = 40,
  categorical_thr = 35,
  verbose = FALSE,
  prob = 0.75
)
}
\arguments{
\item{df}{Data frame containing the predictors.}

\item{imp_vec}{Named numeric vector of importance values.}

\item{predictors}{Character vector of predictor names (order for the output).}

\item{importance_method}{One of \code{"fixed"}, \code{"net_clust"}, \code{"neg_exp"}.}

\item{quantitative_thr}{Numeric. Threshold for quantitative predictors (default 40).}

\item{categorical_thr}{Numeric. Threshold for categorical predictors (default 35).}

\item{verbose}{Logical. Print verbose diagnostics (default \code{FALSE}).}

\item{prob}{Numeric. Probability cutoff for the neural network (default 0.75).}
}
\value{
Named logical vector (aligned with \code{predictors}),
where \code{TRUE} means "removable".
}
\description{
Classifies predictors as removable or to be kept based on importance values,
using one of:
\itemize{
\item \code{"fixed"} — fixed thresholds for quantitative vs categorical;
\item \code{"net_clust"} — 1D DBSCAN clustering + neural network on high cluster(s);
\item \code{"neg_exp"} — adaptive exponential-decay thresholds depending on \eqn{p}.
}
}
\examples{
imp <- c(x1 = 10, x2 = 50)
df  <- data.frame(x1 = 1:10, x2 = letters[1:10])
evaluate_importance(df, imp, predictors = names(imp), importance_method = "fixed")


}

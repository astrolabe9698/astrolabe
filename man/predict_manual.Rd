% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{predict_manual}
\alias{predict_manual}
\title{Manual prediction with a dense neural network}
\usage{
predict_manual(
  X,
  weights_dense,
  activations = "relu",
  last_activation = NULL,
  verbose = FALSE
)
}
\arguments{
\item{X}{Numeric input matrix, with rows = observations and columns = features.}

\item{weights_dense}{A list of length \code{2*L} containing, for each layer,
the weight matrix (\code{W}) and the bias vector (\code{b}), in the order \verb{[W1, b1, W2, b2, ...]}.}

\item{activations}{Either:
\itemize{
\item a single activation name/function (recycled for all layers), or
\item a vector/list of length \code{L} with one activation per layer.
Supported names: \code{"relu"}, \code{"sigmoid"}, \code{"tanh"}, or custom functions.
}}

\item{last_activation}{Optional. If provided, overrides the activation
of the last layer.}

\item{verbose}{Logical; if \code{TRUE}, prints layer shapes and debugging info.}
}
\value{
A numeric matrix (or vector if 1D) with the output of the network.
}
\description{
Performs a forward pass of a feed-forward neural network
given a list of weights and biases, applying the specified
activation functions at each layer.
}

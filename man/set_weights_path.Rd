% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights_utils.R
\name{set_weights_path}
\alias{set_weights_path}
\title{Configure a custom path for the neural-network weights (.rds)}
\usage{
set_weights_path(path)
}
\arguments{
\item{path}{\code{character(1)}. Absolute or relative path to a \code{.rds} file
that exists and is readable.}
}
\value{
Invisibly returns the path (invisible character scalar).
}
\description{
Sets (and validates) a custom file path for the pre-saved weights object
used by the package. This function does \strong{not} read the file immediately;
it only stores the path and clears the in-memory cache so the next call to
\code{\link[=get_weights_dense]{get_weights_dense()}} will reload from disk.

Package-default behavior looks for a file named
\code{inst/models/weights_nn_model.rds} at install time and resolves it via
\code{\link[base:system.file]{base::system.file()}}. Use this function if you want to override that path.
}
\examples{
\dontrun{
# Point to a custom weights file:
set_weights_path("/path/to/weights_nn_model.rds")

# Then use it (will be loaded lazily on first call):
W <- get_weights_dense()
}
}
\seealso{
\code{\link[=get_weights_dense]{get_weights_dense()}}
}

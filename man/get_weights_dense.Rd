% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights_utils.R
\name{get_weights_dense}
\alias{get_weights_dense}
\title{Retrieve the neural-network weights (lazy-loaded, cached)}
\usage{
get_weights_dense()
}
\value{
The R object read from the \code{.rds} file (typically a list/matrix
of layer weights).
}
\description{
Returns the weights object (previously saved with \code{\link[base:readRDS]{base::saveRDS()}})
used by functions in this package. The object is loaded on first call and
cached in memory for subsequent calls. If a custom path was set via
\code{\link[=set_weights_path]{set_weights_path()}}, that file is used. Otherwise, the function looks for
\code{inst/models/weights_nn_model.rds} bundled with the package using
\code{\link[base:system.file]{base::system.file()}}.
}
\details{
This function avoids reading files at top-level (package load time) to keep
\code{devtools::load_all()} and \code{devtools::document()} robust. The cache is
stored in a private environment and persists while the package is loaded.
}
\examples{
\dontrun{
# Default (uses the file inside the installed package, if present):
W <- get_weights_dense()

# After setting a custom path:
set_weights_path("/path/to/weights_nn_model.rds")
W2 <- get_weights_dense()
}
}
\seealso{
\code{\link[=set_weights_path]{set_weights_path()}}
}
